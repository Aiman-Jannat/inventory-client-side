import useUpdateCheck from "../hooks/useUpdateCheck";


const Off = ({item}) => {
    const data = useUpdateCheck({item});
    return (
        <div>
            
        </div>
    );
};

export default Off;